"""Intermediate certificates that some authorities fail to send.

Every certificate in this directory must already chain to a root in the standard trust store, so
including it grants no new trust. It only supplies a link the server omitted.
"""
