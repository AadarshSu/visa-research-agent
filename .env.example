# OpenAI extraction is optional. Keep secrets only in .env, never in this template.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Fixture sources never access government websites. Choose fixture or openai extraction separately.
SOURCE_MODE=fixtures
EXTRACTION_MODE=fixture
CACHE_DIRECTORY=var/cache
