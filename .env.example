# Phase 2 will use these values for structured extraction.
OPENAI_API_KEY=
OPENAI_MODEL=

# Use fixtures for deterministic, offline development; use live only explicitly.
SOURCE_MODE=fixtures
CACHE_DIRECTORY=var/cache

